# Coding-Agent Prompt Library

These are reusable execution templates. `AGENTS.md`, canonical specifications and `tasks/CURRENT.md` always remain higher-priority repository context.

Use the smallest prompt matching the work type. Do not paste the entire project documentation into every prompt; tell the agent to read the canonical files.
