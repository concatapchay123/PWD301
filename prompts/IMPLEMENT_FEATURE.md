# Implement Current Feature

Read `AGENTS.md`, `tasks/CURRENT.md`, relevant canonical specification, database docs, API contract and acceptance criteria.

Implement only the current task. First inspect existing code repository-wide for reusable code and dependencies. Produce the smallest correct implementation; do not invent business rules or bypass security/audit/concurrency requirements.

Required completion:

- code + migrations if needed;
- validation/authz/error handling;
- tests including negative/edge cases;
- relevant documentation/traceability update only when contracts changed;
- run verification and report unrun checks;
- final report in AGENTS.md sections A–G.
