# Runtime foundation only. Feature-specific dependencies should be added by the task that needs them.
# Exact pins should be frozen after the first verified bootstrap install.
Flask>=3.0,<4
Flask-SQLAlchemy>=3.1,<4
Flask-WTF>=1.2,<2
Flask-Login>=0.6,<1
Flask-Migrate>=4.0,<5
SQLAlchemy>=2.0,<3
pyodbc>=5.1,<6
python-dotenv>=1.0,<2
email-validator>=2.1,<3

# Intentionally deferred until the relevant task confirms the implementation library:
# - JWT helper library
# - Gemini SDK
# - background-job/queue technology
# - ClamAV client integration package
